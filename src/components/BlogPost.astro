---
import Card from "./Card.astro";
import { formatDate } from "../scripts/formatDate";

const { url, title, description, pubDate, tags } = Astro.props;
---

<Card href={url} title={title} body={description} date={formatDate(pubDate)} tags={tags} />
