---
import Card from "./Card.astro";

const { url, title, description, image } = Astro.props;
---

<Card href={url} title={title} body={description} image={image} />

<style>
  li {
    max-width: 5rem;
  }
</style>
