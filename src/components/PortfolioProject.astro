---
import Card from "./Card.astro";

const { url, title, description, image, attributes } = Astro.props;
---

<Card
  href={url}
  title={title}
  body={description}
  image={image}
  attributes={attributes}
/>
